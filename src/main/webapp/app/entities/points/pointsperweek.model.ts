import { BaseEntity } from './../../shared';

export class Pointspeerweek implements BaseEntity {
    constructor(
        public id?: any,
        public fecha?: any,
        public week?: any,

    ) {
    }
}
